{
  "name": "myexpenses",
  "version": "1.0.0",
  "description": "An expenses Tracker app to calculate the transactions",
  "main": "server.js",
  "scripts": {
    "server": "node server.js",
    "nodemon": "nodemon server",
    "debugg": "nodemon --inspect server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Karnadeep Rana",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "config": "^3.3.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-validator": "^6.6.0",
    "pg": "^8.2.1",
    "sequelize": "^5.21.13"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
